import { Container, Stack } from "@mui/material";

export const AdjustmentsRoutePage = () => {
  return (
    <Stack spacing={2} alignItems="center">
      <Container maxWidth="sm">
        <Stack spacing={3}></Stack>
      </Container>
    </Stack>
  );
};
