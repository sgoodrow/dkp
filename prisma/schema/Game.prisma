model Game {
    // Common
    id Int @id @default(autoincrement())

    // Properties
    name String @unique

    // Relationships
    guilds  Guild[]
    races   CharacterRace[]
    classes CharacterClass[]
    items   Item[]
}
